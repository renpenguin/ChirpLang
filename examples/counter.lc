func count_from(starting_value int) {
	var counter = starting_value

	print("Counting from", counter)

	forever {
		counter += 1
		print("We have now counted", counter, "times")
		time:sleep(0.2)
	}
}

count_from(0 - 1)
